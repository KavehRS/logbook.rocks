---
description: Daily SEO agent — rank #1 for four pillars and #1–2 for every ascent report
alwaysApply: true
---

# Daily SEO agent

Goal: make https://logbook.rocks the **#1 Persian result** for:

- راهنمای صعود
- گزارش صعود
- کوهنوردی فنی
- کوهنوردی در ایران

and **#1 or #2** for every published `/logbook/` report on that climb’s queries (see `_seo/ranking-targets.yml`). Related queries follow the same people-first rules.

## When this applies

- **24-hour SEO automation** (`.cursor/automations/daily-seo-prompt.md` + `.github/workflows/daily-seo-agent.yml`)
- **GMT news-wire cycle** (00/06/12/18) — whole-site SEO is job 4 of `.cursor/skills/news-wire/SKILL.md`
- **45-minute SEO + AI-source watch** (`.cursor/skills/seo-ai-source-watch/SKILL.md`)
- Any request to audit, improve, or update site SEO or AI citation / source listing

## Required workflow

1. Read `AGENTS.md` and `.cursor/skills/daily-seo-audit/SKILL.md`.
2. Pull latest authoritative SEO guidance (Google Search Central, Bing Webmaster Guidelines, schema.org) before changing markup.
3. Audit the live site and the repo (meta, canonical, sitemap, robots, structured data, internal links, images, Core Web Vitals proxies, crawl errors).
4. Review `_seo/ranking-targets.yml`: four pillars + every `_logbook` report’s `{peak}` queries. Update `_seo/ranking-snapshot.md`.
5. Implement only evidence-based improvements; keep PRs narrowly scoped.
6. Run `bundle exec jekyll build` and confirm `_drafts/` / `.cursor` are not published.
7. Open PR, then merge after a clean build when the change is high-confidence.

## Ranking priorities (logbook first)

1. Unique live HTML per URL (no catch-all homepage duplicate)
2. Unique, factual ascent reports with strong titles/descriptions/tags (peak name in the title)
3. Structured data (`BlogPosting`/`Article` + mountain/place when peak data exists; CollectionPage `about` includes the four pillars)
4. Internal links between related climbs and the `/logbook/` hub
5. Accurate Open Graph / Twitter images (prefer climb photos over logo)
6. Technical hygiene: broken links, missing alts, wrong `lang`/`dir`, sitemap noise

Do not scrape Google SERPs. Prefer Search Console when the owner has connected it.

## Climb-report content note

When touching `_logbook` content while doing SEO work:
- do not add guessed coordinates, elevations, or species lists; omit unsourced peak fields (`.cursor/rules/logbook-facts.mdc`)
- keep the required weather block from **accurate sources only** (Open-Meteo; Mountain-Forecast only if peak has its own page; Meteoblue when reliable — omit distant proxies)
- keep gear aligned to date/forecast
- keep `categories` on the discipline taxonomy in `_data/logbook_disciplines.yml` (including `technical-mountaineering` / کوهنوردی فنی and `winter-ascent` / صعود زمستانه)
- ridge/gendarme/alpine hand-and-foot → `technical-mountaineering`; rock/wall only for true rock/wall
- keep images under `assets/mount/logbook/<exact-url-slug>/`
- if touching weather numbers that changed noticeably, keep `## چالش‌های برنامه` in sync
- hub `/logbook/` stays chronological; related UI stays flat `گزارش‌های مرتبط :` (no agent blurbs / category subheads)
- reader-facing dates stay Jalali (`_includes/jalali-date.html`); do not revert footer/post/hub dates to English Gregorian
- never publish agent/implementation notes into live page copy
per `.cursor/rules/logbook-reports.mdc` and `.cursor/rules/logbook-assets.mdc`. Do not invent vague categories.

## 45-minute SEO + AI-source watch

Cadence: every 45 minutes until the owner pauses it. Watch **official** SEO docs and AI-citation materials (`_seo/guidance-sources.yml`), then apply **technical** site signals only (`robots.txt`, `/llms.txt`, WebMCP, IndexNow, `rel=describedby`, crawl headers).

This is not a Google rank refresh and not a scrape of other clubs’ reports.

**Never edit existing `_news/*.md` or `_logbook/*.md` bodies, titles, descriptions, tags, or categories** on this watch. Research notes go in unpublished `_seo/watch-inbox.md`.

## Hard limits

- Do not duplicate published report prose for the same peak.
- Do not rewrite published logbook/news copy to chase SEO or AI citations.
- Do not keyword-stuff titles or body copy.
- Do not fabricate climb details to chase rankings.
- Do not scrape google.com / bing.com result pages.
- Do not remove `noindex` from archive/projects/404 without explicit user request.
- Do not change analytics IDs or domain/`CNAME` casually.
